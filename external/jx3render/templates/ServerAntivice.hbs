<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>服务器诛恶事件</title>
  <style>
    @font-face {
      font-family: "DengXian";
      src: local("DengXian"), local("SimHei");
    }

    /* Reset & Base */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      width: 1400px;
      margin: 0 auto;
      font-family: "Microsoft YaHei", "Heiti SC", sans-serif;
      background-color: #f0f0f0;
      /* Neo Gray Background */
      background-image:
        linear-gradient(90deg, #bebebe 1px, transparent 1px),
        linear-gradient(#bebebe 1px, transparent 1px);
      background-size: 40px 40px;
      color: #000;
      padding: 40px;
    }

    .container {
      /* min-height: 100vh; */
    }

    /* Header Section (Unified with ActiveList/TradeRecords) */
    .page-header {
      margin-bottom: 30px;
      text-align: left;
      /* Left align for that brutalist feel */
      position: relative;
    }

    .page-title {
      font-size: 36px;
      font-weight: 900;
      color: #000;
      display: inline-block;
      background: #ffeb3b;
      /* Safety Yellow */
      padding: 15px 30px;
      border: 4px solid #000;
      box-shadow: 8px 8px 0px 0px #000;
      transform: rotate(-2deg);
      text-transform: uppercase;
      letter-spacing: -1px;
    }

    .page-title span {
      display: block;
      /* transform: skew(10deg); */
    }

    .page-subtitle {
      margin-top: 15px;
      display: inline-block;
      background: #000;
      color: #fff;
      padding: 8px 16px;
      border: 3px solid #000;
      font-weight: 900;
      font-size: 16px;
      box-shadow: 4px 4px 0px 0px rgba(0, 0, 0, 0.3);
      transform: rotate(1deg);
    }

    /* Card Container */
    .card {
      background: #ffffff;
      border: 4px solid #000;
      box-shadow: 12px 12px 0px 0px #000;
      padding: 20px;
    }

    /* Table Styling */
    .table-container {
      width: 100%;
      overflow-x: auto;
    }

    table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0;
    }

    thead {
      background-color: #000;
    }

    th {
      padding: 15px;
      text-align: center;
      font-weight: 900;
      font-size: 20px;
      color: #fff;
      border: 3px solid #000;
      white-space: nowrap;
      text-transform: uppercase;
    }

    /* Row Hover and Striping */
    tbody tr:nth-child(odd) {
      background-color: #e0f7fa;
      /* Light Cyan */
    }

    tbody tr:nth-child(even) {
      background-color: #fce4ec;
      /* Light Pink */
    }

    td {
      padding: 15px;
      text-align: center;
      vertical-align: middle;
      font-size: 20px;
      border-right: 3px solid #000;
      border-bottom: 3px solid #000;
      color: #000;
      font-weight: bold;
    }

    td:first-child {
      border-left: 3px solid #000;
    }

    /* Column Specific Styles */
    .time-cell {
      font-family: "DengXian", "Microsoft YaHei", sans-serif;
      font-weight: 900;
      color: #000;
      font-size: 24px;
      background: #fff;
      border: 2px solid #000;
      padding: 5px 15px;
      display: inline-block;
      box-shadow: 3px 3px 0px 0px #000;
      min-width: 120px;
    }

    .location-cell {
      font-weight: 900;
      font-size: 24px;
      color: #000;
      text-decoration: underline;
      text-decoration-thickness: 2px;
    }

    .site-cell {
      color: #000;
    }

    /* Stage Badge */
    .stage-wrapper {
      display: inline-flex;
      align-items: center;
      gap: 10px;
      padding: 6px 14px;
      border: 2px solid #000;
      background: #fff;
      font-size: 18px;
      font-weight: 900;
      box-shadow: 3px 3px 0px 0px #000;
    }

    .status-dot {
      width: 12px;
      height: 12px;
      background-color: #000;
      display: inline-block;
      border: 2px solid #000;
    }

    /* Highlight Progress Stages */
    .stage-active .status-dot {
      background-color: #1dd1a1;
      /* Green */
    }

    .stage-active {
      color: #000;
      background: #fff;
    }
  </style>
</head>

<body>
  <div class="container">

    <div class="page-header">
      <div class="page-title">
        <span>JX3 ANTIVICE TRACKER</span>
      </div>
      <br>
      <div class="page-subtitle">{{server}}</div>
    </div>

    <div class="card">
      <div class="table-container">
        <table>
          <thead>
            <tr>
              <th style="width: 30%">地图</th>
              <th style="width: 40%">触发时间</th>
              <th style="width: 30%">间隔</th>
            </tr>
          </thead>
          <tbody>
            {{#each data}}
            <tr>
              <td>
                <span class="location-cell">{{map_name}}</span>
              </td>
              <td>
                <span class="time-cell">{{formatTime time "YYYY-MM-DD HH:mm"}}</span>
              </td>
              <td>
                <div class="stage-wrapper">
                  <span>{{formatInterval time}}</span>
                </div>
              </td>
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>

</html>
