<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>服务器烟花统计</title>
  <style>
    @font-face {
      font-family: "DengXian";
      src: local("DengXian"), local("SimHei");
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      width: 1600px;
      margin: 0 auto;
      font-family: "Microsoft YaHei", "Heiti SC", sans-serif;
      background-color: #fceef5;
      /* Base Pink */
      background-image: radial-gradient(#ffc0cb 15%, transparent 16%), radial-gradient(#ffc0cb 15%, transparent 16%);
      background-size: 20px 20px;
      background-position: 0 0, 10px 10px;
      color: #2d3436;
      padding-bottom: 40px;
    }

    .container {
      padding: 40px;
    }

    /* Page Header */
    .page-header {
      margin-bottom: 40px;
      text-align: center;
      position: relative;
      z-index: 10;
    }

    .page-title {
      font-size: 42px;
      font-weight: 900;
      color: #e056fd;
      text-shadow: 2px 2px 0px #fff;
      display: inline-block;
      background: #fff;
      padding: 12px 50px;
      border-radius: 60px;
      box-shadow: 0 6px 20px rgba(224, 86, 253, 0.2);
      transform: skew(-10deg);
    }

    .page-title span {
      display: block;
      transform: skew(10deg);
    }

    .page-subtitle {
      margin-top: 15px;
      display: inline-block;
      color: #fff;
      background: #fd79a8;
      padding: 5px 20px;
      border-radius: 20px;
      font-weight: bold;
      box-shadow: 0 4px 10px rgba(253, 121, 168, 0.3);
    }

    /* Grid Layout */
    .record-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 20px;
    }

    /* Card Styling */
    .record-card {
      background: #fff;
      border-radius: 20px;
      padding: 20px;
      position: relative;
      box-shadow: 0 8px 20px rgba(236, 72, 153, 0.1);
      display: flex;
      flex-direction: column;
      gap: 15px;
      border: 2px solid transparent;
      transition: all 0.3s ease;
    }

    .record-card:hover {
      transform: translateY(-5px);
      box-shadow: 0 12px 30px rgba(236, 72, 153, 0.2);
      border-color: #fbcfe8;
    }

    /* Card Gradient Top Bar */
    .record-card::before {
      content: "";
      position: absolute;
      top: 0;
      left: 20px;
      right: 20px;
      height: 4px;
      background: linear-gradient(90deg, #fd79a8, #e056fd);
      border-radius: 0 0 10px 10px;
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 5px;
    }

    .firework-badge {
      font-size: 18px;
      font-weight: 900;
      color: #d63031;
      background: #fff0f6;
      padding: 6px 12px;
      border-radius: 12px;
      display: flex;
      align-items: center;
      gap: 5px;
    }

    .firework-icon {
      width: 18px;
      height: 18px;
      fill: #fd79a8;
    }

    .count-badge {
      font-size: 24px;
      font-weight: 900;
      color: #6c5ce7;
      font-family: "DengXian";
    }

    .count-badge small {
      font-size: 14px;
      color: #a29bfe;
      margin-right: 2px;
    }

    /* People Info */
    .people-info {
      background: #fdf2f8;
      border-radius: 12px;
      padding: 15px;
      display: flex;
      flex-direction: column;
      gap: 8px;
      border: 1px dashed #f8a5c2;
    }

    .people-row {
      display: flex;
      align-items: center;
      gap: 10px;
      font-size: 16px;
    }

    .role-tag {
      font-size: 12px;
      font-weight: bold;
      color: #fff;
      padding: 2px 8px;
      border-radius: 6px;
      min-width: 24px;
      text-align: center;
    }

    .role-sender {
      background: #fd79a8;
    }

    .role-receiver {
      background: #74b9ff;
    }

    .role-name {
      font-weight: bold;
      color: #2d3436;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    /* Footer */
    .card-footer {
      display: flex;
      justify-content: flex-end;
      align-items: center;
      margin-top: auto;
    }

    .time-tag {
      font-size: 14px;
      color: #b2bec3;
      font-weight: bold;
      display: flex;
      align-items: center;
      gap: 5px;
      background: #f1f2f6;
      padding: 4px 10px;
      border-radius: 20px;
    }

    .time-icon {
      width: 14px;
      height: 14px;
      fill: #b2bec3;
    }
  </style>
</head>

<body>
  <div class="container">

    <div class="page-header">
      <div class="page-title">
        <span>FIREWORKS LOG</span>
      </div>
      <br>
      <div class="page-subtitle">服务器烟花统计</div>
    </div>

    <div class="record-grid">
      {{#each this}}
      <div class="record-card">

        <div class="card-header">
          <div class="firework-badge">
            <svg class="firework-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd"
                d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z"
                clip-rule="evenodd" transform="scale(1.2) rotate(45)" />
              <!-- Reusing a simpler icon for visual -->
              <path
                d="M9.049 2.927c.3-.921 1.603-.921 1.902 0l1.07 3.292a1 1 0 00.95.69h3.462c.969 0 1.371 1.24.588 1.81l-2.8 2.034a1 1 0 00-.364 1.118l1.07 3.292c.3.921-.755 1.688-1.54 1.118l-2.8-2.034a1 1 0 00-1.175 0l-2.8 2.034c-.784.57-1.838-.197-1.539-1.118l1.07-3.292a1 1 0 00-.364-1.118L2.98 8.72c-.783-.57-.38-1.81.588-1.81h3.461a1 1 0 00.951-.69l1.07-3.292z" />
            </svg>
            {{name}}
          </div>
          <div class="count-badge"><small>x</small>{{count}}</div>
        </div>

        <div class="people-info">
          <div class="people-row">
            <span class="role-tag role-sender">赠</span>
            <span class="role-name">{{sender}}</span>
          </div>
          <div class="people-row">
            <span class="role-tag role-receiver">予</span>
            <span class="role-name">{{receive}}</span>
          </div>
        </div>

        <div class="card-footer">
          <div class="time-tag">
            <svg class="time-icon" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd"
                d="M10 18a8 8 0 100-16 8 8 0 000 16zm1-12a1 1 0 10-2 0v4a1 1 0 00.293.707l2.828 2.829a1 1 0 101.415-1.415L11 9.586V6z"
                clip-rule="evenodd" />
            </svg>
            {{formatTime time "MM-DD HH:mm"}}
          </div>
        </div>

      </div>
      {{/each}}
    </div>

  </div>
</body>

</html>