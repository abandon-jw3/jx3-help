<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>师父信息</title>
  <style>
    /* Reset & Base */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      width: 1400px;
      margin: 0 auto;
      font-family: "Courier New", Courier, monospace, "Microsoft YaHei";
      background-color: #f4d03f;
      background-image:
        radial-gradient(circle, #000 2px, transparent 2.5px);
      background-size: 30px 30px;
      color: #000;
      padding: 50px;
    }

    .container {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Header Section */
    .page-header {
      margin-bottom: 40px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      background: #fff;
      border: 4px solid #000;
      box-shadow: 10px 10px 0px #000;
      padding: 30px;
      transform: rotate(1deg);
    }

    .page-title {
      font-size: 48px;
      font-weight: 900;
      text-transform: uppercase;
      line-height: 1;
      font-family: "Impact", "SimHei", sans-serif;
      text-shadow: 3px 3px 0 #d35400;
    }

    .subtitle {
      font-size: 24px;
      background: #000;
      color: #fff;
      padding: 5px 15px;
      margin-top: 5px;
      display: inline-block;
      font-family: monospace;
      transform: skew(-10deg);
    }

    /* Grid Layout */
    .teacher-grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 30px;
    }

    /* Teacher Card */
    .teacher-card {
      background: #fff;
      border: 4px solid #000;
      box-shadow: 8px 8px 0px #000;
      position: relative;
      display: flex;
      flex-direction: column;
      transition: all 0.2s;
    }

    .teacher-card:hover {
      transform: translate(-4px, -4px);
      box-shadow: 12px 12px 0px #000;
    }

    /* Card Header */
    .card-header {
      background: #000;
      color: #fff;
      padding: 15px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      border-bottom: 4px solid #000;
    }

    .role-name {
      font-size: 24px;
      font-weight: 900;
      font-family: "SimHei", sans-serif;
    }

    .role-level {
      font-size: 16px;
      background: #ffeaa7;
      /* Yellow */
      color: #000;
      padding: 4px 8px;
      border: 2px solid #fff;
      font-weight: bold;
      font-family: monospace;
    }

    /* Card Body */
    .card-body {
      padding: 20px;
      flex: 1;
      display: flex;
      flex-direction: column;
      gap: 15px;
      position: relative;
    }

    .tag-row {
      display: flex;
      flex-wrap: wrap;
      gap: 10px;
      position: relative;
      z-index: 2;
    }

    .tag {
      font-size: 14px;
      padding: 6px 10px;
      border: 2px solid #000;
      font-weight: bold;
      box-shadow: 2px 2px 0 #000;
    }

    .tag.force {
      background: #74b9ff;
      /* Blue */
      color: #000;
    }

    .tag.body {
      background: #fab1a0;
      /* Pink/Orange */
      color: #000;
    }

    .tag.camp {
      background: #ff7675;
      /* Red */
      color: #000;
    }

    .tag.tong {
      background: #55efc4;
      /* Mint */
      color: #000;
      max-width: 100%;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
    }

    /* Comment Section */
    .comment-box {
      margin-top: auto;
      background: #f0f0f0;
      padding: 10px;
      border: 2px solid #000;
      font-size: 14px;
      color: #000;
      position: relative;
      z-index: 2;
    }

    .comment-box::before {
      content: "❝";
      font-size: 20px;
      margin-right: 5px;
      font-weight: bold;
    }

    /* Index Badge */
    .card-index {
      position: absolute;
      bottom: 10px;
      right: 10px;
      font-size: 80px;
      font-weight: 900;
      color: rgba(0, 0, 0, 0.05);
      line-height: 1;
      pointer-events: none;
      font-family: "Impact", sans-serif;
      z-index: 1;
    }

    /* Footer decoration */
    .footer-deco {
      margin-top: 40px;
      height: 20px;
      background: repeating-linear-gradient(45deg,
          #000,
          #000 10px,
          #fff 10px,
          #fff 20px);
      border: 4px solid #000;
    }
  </style>
</head>

<body>
  <div class="container">

    <div class="page-header">
      <div class="page-title">
        <span>{{server}} 师父名录</span>
        <span class="subtitle">MASTER LIST</span>
      </div>
      <div style="font-size: 18px; font-weight: bold;">
        TOTAL: {{data.length}}
      </div>
    </div>

    <div class="teacher-grid">
      {{#each data}}
      <div class="teacher-card">
        <div class="card-header">
          <div class="role-name">{{roleName}}</div>
          <div class="role-level">Lv.{{roleLevel}}</div>
        </div>

        <div class="card-body">
          <div class="card-index">{{inc @index}}</div>

          <div class="tag-row">
            <span class="tag force">{{forceName}}</span>
            <span class="tag body">{{bodyName}}</span>
          </div>

          <div class="tag-row">
            <span class="tag camp">阵营: {{campName}}</span>
          </div>

          {{#if tongName}}
          <div class="tag-row">
            <span class="tag tong">帮会: {{tongName}}</span>
          </div>
          {{/if}}

          {{#if comment}}
          <div class="comment-box">
            {{comment}}
          </div>
          {{else}}
          <div class="comment-box" style="color:#636e72; font-style:italic; border-style: dashed;">
            NO COMMENT
          </div>
          {{/if}}
        </div>
      </div>
      {{/each}}
    </div>

    <div class="footer-deco"></div>
  </div>
</body>

</html>