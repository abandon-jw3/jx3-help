<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>全服掉落信息</title>
  <link href="http://localhost:3000/bootstrap-5.3.0-alpha1-dist/js/bootstrap.min.css" rel="stylesheet" />
  <link rel="stylesheet" href="http://localhost:3000/bootstrap-icons-1.13.1/bootstrap-icons.min.css">
  <script src="http://localhost:3000/bootstrap-5.3.0-alpha1-dist/js/bootstrap.min.js"></script>

</head>

<body>
  <div class="container-fluid py-4">
    <div class="card shadow-sm">
      <div class="card-header bg-light text-dark">
        <span>
          <i class="bi bi-emoji-sunglasses-fill"></i>
          <span>{{name}}</span>
        </span>
        <span>
          <i class="bi bi-grid-3x3-gap-fill"></i>
          <span>掉落统计</span>
        </span>
        <span>
          <i class="bi bi-cursor-fill"></i>
          <span>{{server}}</span>
        </span>
      </div>
      <div class="card-body p-0 w-100">
        <div class="table-responsive">
          <table class="table table-hover table-striped mb-0 w-100">
            <thead class="table-light">
              <tr>
                <th scope="col" class="text-center" style="width: 25%;">服务器</th>
                <th scope="col" class="text-center" style="width: 25%;">角色名称</th>
                <th scope="col" class="text-center" style="width: 25%;">地图名称</th>
                <th scope="col" class="text-center" style="width: 25%;">时间</th>
              </tr>
            </thead>
            <tbody>
              {{#each data}}
              <tr>
                <td class="text-center align-middle">
                  <div class="text-dark">{{server}}</div>
                </td>
                <td class="text-center align-middle">
                  <div class="text-dark">{{role_name}}</div>
                </td>
                <td class="text-center align-middle">
                  <div class="text-dark">{{map_name}}</div>
                </td>
                <td class="text-center align-middle">
                  <div class="text-dark">{{formatTime time}}</div>
                </td>
              </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>

  <style>
    body {
      font-family: "Microsoft YaHei", Arial, sans-serif;
    }

    .card {
      border-radius: 15px;
      border: none;
    }

    .card-header {
      border-radius: 15px 15px 0 0 !important;
      padding: 16px;
      display: flex;
      justify-content: space-evenly;
      align-items: center;
      font-size: 22px;
    }

    .table {
      font-size: 22px;
    }

    .table thead th {
      font-weight: 600;
      border-bottom: 2px solid #dee2e6;
      padding: 16px;
    }

    .table tbody td {
      padding: 16px;
    }

    .table-hover tbody tr:hover {
      background-color: rgba(102, 126, 234, 0.1);
    }

    .badge {
      padding: 0.5rem 0.75rem;
      font-weight: 500;
    }

    .card-footer {
      background-color: #f8f9fa;
      border-radius: 0 0 15px 15px !important;
      padding: 1rem;
    }
  </style>
</body>

</html>
