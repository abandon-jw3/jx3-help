<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>角色奇遇记录</title>
  <style>
    @font-face {
      font-family: "DengXian";
      src: local("DengXian"), local("SimHei");
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      width: 1400px;
      /* Using 1400px to match other 3 templates */
      margin: 0 auto;
      font-family: "Courier New", Courier, monospace, "Microsoft YaHei";
      background-color: #f4d03f;
      background-image:
        radial-gradient(circle, #000 2px, transparent 2.5px);
      background-size: 30px 30px;
      color: #000;
      padding: 50px;
    }

    .container {
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* Header Styling */
    .page-header {
      margin-bottom: 50px;
      background: #fff;
      border: 4px solid #000;
      box-shadow: 10px 10px 0px #000;
      padding: 20px 60px;
      transform: rotate(-1deg);
      display: inline-block;
    }

    .page-header h1 {
      font-size: 48px;
      margin: 0;
      font-weight: 900;
      text-transform: uppercase;
      font-family: "Impact", "SimHei", sans-serif;
      text-shadow: 3px 3px 0 #d35400;
      letter-spacing: 2px;
    }

    /* Grid Styling */
    .grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 30px;
      width: 100%;
    }

    /* Card Styling */
    .card {
      background: #fff;
      border: 4px solid #000;
      box-shadow: 8px 8px 0px #000;
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
      transition: all 0.1s;
      position: relative;
    }

    .card:hover {
      transform: translate(4px, 4px);
      box-shadow: 4px 4px 0px #000;
    }

    .qiyu-img {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      background: #ebf5fb;
      border: 2px solid #000;
      padding: 10px;
    }

    .qiyu-img img {
      width: 100%;
      height: auto;
      object-fit: contain;
      filter: none;
    }

    /* Time Badge */
    .time {
      font-size: 20px;
      /* Slightly larger for impact */
      font-weight: 900;
      color: #000;
      background-color: #ffeaa7;
      /* Yellow base */
      padding: 10px 20px;
      border: 3px solid #000;
      box-shadow: 4px 4px 0px #000;
      font-family: "Consolas", monospace, "SimHei";
      white-space: nowrap;
      transform: skew(-5deg);
    }

    /* Alternating colors */
    .card:nth-child(2n) .time {
      background-color: #ff7675;
      /* Salmon */
    }

    .card:nth-child(3n) .time {
      background-color: #74b9ff;
      /* Blue */
    }

    .card:nth-child(4n) .time {
      background-color: #55efc4;
      /* Mint */
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="page-header">
      <h1># 角色奇遇记录 #</h1>
    </div>
    <div class="grid">
      {{#each this}}
      <div class="card">
        <div class="qiyu-img">
          <img src="http://localhost:5140/jx3assets/images/qiyu/{{event}}.png" alt="{{event}}" />
        </div>
        <div class="time">{{default (formatTime time "YYYY-MM-DD HH:mm") "太久啦！已经忘记了！"}}</div>
      </div>
      {{/each}}
    </div>
  </div>
</body>

</html>