<!DOCTYPE html>
<html lang="zh-CN">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>活动日历</title>
    <style>
        body {
            margin: 0;
            padding: 20px;
            font-family: 'Microsoft YaHei', 'SimSun', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
        }

        .calendar-container {
            background: rgba(255, 255, 255, 0.95);
            border-radius: 15px;
            padding: 25px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
            max-width: 1200px;
            margin: 0 auto;
        }

        .calendar-title {
            text-align: center;
            font-size: 28px;
            font-weight: bold;
            color: #333;
            margin-bottom: 25px;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.1);
        }

        .calendar-grid {
            display: grid;
            grid-template-columns: repeat(7, 1fr);
            gap: 8px;
            margin-bottom: 20px;
        }


        .day-cell {
            background: #fff;
            border: 2px solid #e0e0e0;
            border-radius: 10px;
            padding: 8px;
            min-height: 120px;
            display: flex;
            flex-direction: column;
            transition: all 0.3s ease;
            box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        }

        .day-cell:hover {
            transform: translateY(-2px);
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
        }

        .day-number {
            font-size: 14px;
            font-weight: bold;
            color: #333;
            text-align: center;
            margin-bottom: 6px;
            background: linear-gradient(45deg, #667eea, #764ba2);
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .activity-item {
            margin-bottom: 4px;
            font-size: 11px;
            line-height: 1.3;
        }

        .war-activity {
            color: #e74c3c;
            font-weight: bold;
        }

        .battle-activity {
            color: #f39c12;
            font-weight: bold;
        }

        .school-activity {
            color: #27ae60;
            font-weight: bold;
        }

        .card-activity {
            color: #9b59b6;
            font-weight: bold;
        }

        .luck-activity {
            color: #3498db;
            font-weight: bold;
        }

        .today {
            background: linear-gradient(135deg, #ffd700, #ffed4e) !important;
            border-color: #f39c12 !important;
            transform: scale(1.05);
        }

        .footer {
            text-align: center;
            margin-top: 20px;
            color: #666;
            font-style: italic;
        }

        .footer-line1 {
            font-size: 16px;
            margin-bottom: 5px;
        }

        .footer-line2 {
            font-size: 14px;
        }
    </style>
</head>
<body>
    <div class="calendar-container">
        <div class="calendar-title">活动日历</div>
        <div class="calendar-grid">
            {{#each data}}
            <div class="day-cell {{#if isToday}}today{{/if}}">
                <div class="day-number">{{date}}星期{{week}}</div>

                {{#if war}}
                <div class="activity-item war-activity">大战: {{war}}</div>
                {{/if}}

                {{#if battle}}
                <div class="activity-item battle-activity">战场: {{battle}}</div>
                {{/if}}

                {{#if school}}
                <div class="activity-item school-activity">宗门: {{school}}</div>
                {{/if}}
            </div>
            {{/each}}
        </div>

        <div class="footer">
            <div class="footer-line1">萌萌</div>
            <div class="footer-line2">你一笑,便是春天</div>
        </div>
    </div>
</body>
</html>
