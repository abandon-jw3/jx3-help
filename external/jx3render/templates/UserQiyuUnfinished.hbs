<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>角色未出奇遇</title>
  <style>
    @font-face {
      font-family: "DengXian";
      src: local("DengXian"), local("SimHei");
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      width: 1400px;
      /* Increased to match other templates if needed, or keep 1080? Let's align with others for consistency if possible, but 1080 might be intended for layout. User didn't specify width change, but previous templates were 1400. Let's stick to a wide layout for grid but centered. */
      margin: 0 auto;
      font-family: "Courier New", Courier, monospace, "Microsoft YaHei";
      background-color: #f4d03f;
      background-image:
        radial-gradient(circle, #000 2px, transparent 2.5px);
      background-size: 30px 30px;
      color: #000;
      padding: 50px;
    }

    .container {
      /* Width auto to fit within 1400px body with padding */
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
    }

    /* Header Styling */
    .page-header {
      margin-bottom: 50px;
      background: #fff;
      border: 4px solid #000;
      box-shadow: 10px 10px 0px #000;
      padding: 20px 60px;
      transform: rotate(-1deg);
      display: inline-block;
    }

    .page-header h1 {
      font-size: 48px;
      margin: 0;
      font-weight: 900;
      text-transform: uppercase;
      font-family: "Impact", "SimHei", sans-serif;
      text-shadow: 3px 3px 0 #d35400;
      letter-spacing: 2px;
    }

    /* Grid Styling */
    .grid {
      display: grid;
      grid-template-columns: repeat(4, 1fr);
      gap: 30px;
      width: 100%;
    }

    /* Card Styling */
    .card {
      background: #fff;
      border: 4px solid #000;
      box-shadow: 8px 8px 0px #000;
      padding: 20px;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      gap: 20px;
      transition: all 0.1s;
      position: relative;
    }

    .card:hover {
      transform: translate(4px, 4px);
      box-shadow: 4px 4px 0px #000;
    }

    .qiyu-img {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 100%;
      background: #ebf5fb;
      border: 2px solid #000;
      padding: 10px;
    }

    .qiyu-img img {
      width: 100%;
      height: auto;
      object-fit: contain;
      filter: grayscale(100%) contrast(120%);
      /* Artistic touch: grayscale until hover? Or keeping it clean. Let's keep color but add filter if needed. Standard images might be colorful. */
      filter: none;
    }

    /* Type Badge */
    .type {
      font-size: 24px;
      font-weight: 900;
      color: #000;
      background-color: #ff7675;
      /* Salmon/Pink */
      padding: 10px 30px;
      border: 3px solid #000;
      box-shadow: 4px 4px 0px #000;
      font-family: "SimHei", sans-serif;
      white-space: nowrap;
      transform: skew(-5deg);
    }

    /* Alternating colors for visual interest */
    .card:nth-child(2n) .type {
      background-color: #55efc4;
      /* Mint */
    }

    .card:nth-child(3n) .type {
      background-color: #a29bfe;
      /* Purple */
    }

    .card:nth-child(4n) .type {
      background-color: #ffeaa7;
      /* Yellow */
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="page-header">
      <h1># 角色未出奇遇 #</h1>
    </div>
    <div class="grid">
      {{#each this}}
      <div class="card">
        <div class="qiyu-img">
          <img src="http://localhost:5140/jx3assets/images/qiyu/{{name}}.png" alt="{{name}}" />
        </div>
        <div class="type">{{type}}</div>
      </div>
      {{/each}}
    </div>
  </div>
</body>

</html>