<html lang="zh-CN">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>团队招募信息</title>
    <style>
        /* Reset & Base */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            font-family: "Microsoft YaHei", -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif;
            background-color: #f0f2f5;
            padding: 40px;
            color: #333;
            line-height: 1.5;
            width: 1440px;
        }

        /* Card Container */
        .card {
            max-width: 1400px;
            margin: 0 auto;
            background: #ffffff;
            border-radius: 16px;
            box-shadow: 0 8px 30px rgba(0, 0, 0, 0.08);
            overflow: hidden;
        }

        /* Header */
        .header {
            padding: 24px 32px;
            background: linear-gradient(to right, #f8f9fa, #ffffff);
            border-bottom: 1px solid #eaeaea;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            gap: 8px;
        }

        .header h1 {
            font-size: 28px;
            font-weight: 600;
            color: #2c3e50;
            display: flex;
            align-items: center;
            gap: 12px;
            margin: 0;
        }

        .header-meta {
            font-size: 16px;
            color: #64748b;
            display: flex;
            gap: 24px;
        }

        .header-meta span {
            display: flex;
            align-items: center;
            gap: 6px;
        }

        .icon-team {
            color: #3b82f6;
            width: 32px;
            height: 32px;
        }

        /* Table Styling */
        .table-container {
            width: 100%;
            overflow-x: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            border-spacing: 0;
        }

        thead {
            background-color: #f8fafc;
        }

        th {
            padding: 20px 12px;
            text-align: center;
            font-weight: 600;
            font-size: 18px;
            color: #64748b;
            border-bottom: 2px solid #e2e8f0;
            white-space: nowrap;
        }

        td {
            padding: 16px 12px;
            text-align: center;
            vertical-align: middle;
            font-size: 18px;
            border-bottom: 1px solid #edf2f7;
            color: #475569;
        }

        tbody tr:last-child td {
            border-bottom: none;
        }

        tbody tr:hover {
            background-color: #f8fafc;
        }

        tbody tr:nth-child(even) {
            background-color: #fafafa;
        }

        /* Column Specific Styles */
        .col-index {
            color: #94a3b8;
            font-weight: 500;
            width: 60px;
        }

        .badge-activity {
            display: inline-block;
            padding: 6px 12px;
            background-color: #eff6ff;
            color: #2563eb;
            border-radius: 6px;
            font-weight: 600;
            font-size: 16px;
            border: 1px solid rgba(37, 99, 235, 0.1);
        }

        .text-content {
            text-align: left;
            max-width: 400px;
            overflow: hidden;
            text-overflow: ellipsis;
            white-space: nowrap;
        }

        .text-leader {
            font-weight: 500;
            color: #334155;
        }

        .text-time {
            color: #64748b;
            font-size: 16px;
            font-family: Consolas, Monaco, monospace;
        }

        .badge-cross {
            display: inline-block;
            padding: 4px 8px;
            background-color: #ecfdf5;
            color: #059669;
            border-radius: 4px;
            font-size: 14px;
            border: 1px solid rgba(5, 150, 105, 0.2);
        }

        /* Footer/Bottom Spacing */
        .footer-space {
            height: 16px;
            background: #fff;
        }
    </style>
</head>

<body>
    <div class="card">
        <div class="header">
            <h1>
                <svg class="icon-team" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="currentColor">
                    <path
                        d="M12 2C6.48 2 2 6.48 2 12s4.48 10 10 10 10-4.48 10-10S17.52 2 12 2zm0 3c1.66 0 3 1.34 3 3s-1.34 3-3 3-3-1.34-3-3 1.34-3 3-3zm0 14.2c-2.5 0-4.71-1.28-6-3.22.03-1.99 4-3.08 6-3.08 1.99 0 5.97 1.09 6 3.08-1.29 1.94-3.5 3.22-6 3.22z" />
                </svg>
                {{server}} 团队招募
            </h1>
            <div class="header-meta">
                <span>更新时间：{{formatDate time}}</span>
            </div>
        </div>

        <div class="table-container">
            <table>
                <thead>
                    <tr>
                        <th style="width: 5%">序号</th>
                        <th style="width: 15%">活动名称</th>
                        <th style="width: 8%">等级</th>
                        <th style="width: 12%">团长</th>
                        <th style="width: 8%">人数</th>
                        <th style="width: 32%">招募内容</th>
                        <th style="width: 15%">创建时间</th>
                        <th style="width: 5%">其他</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each data}}
                    <tr>
                        <td class="col-index">{{inc @index}}</td>
                        <td>
                            <span class="badge-activity">{{activity}}</span>
                        </td>
                        <td>{{level}}</td>
                        <td class="text-leader">{{leader}}</td>
                        <td>{{number}}/{{maxNumber}}</td>
                        <td class="text-content" title="{{content}}">{{content}}</td>
                        <td>
                            <span class="text-time">{{formatDate createTime}}</span>
                        </td>
                        <td>
                            {{#if crossServer}}
                            <span class="badge-cross">跨服</span>
                            {{/if}}
                        </td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        <div class="footer-space"></div>
    </div>
</body>

</html>