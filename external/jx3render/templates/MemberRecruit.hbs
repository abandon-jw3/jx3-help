<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>团队招募信息</title>
  <style>
    @font-face {
      font-family: "DengXian";
      src: local("DengXian"), local("SimHei");
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      width: 1400px;
      margin: 0 auto;
      font-family: "Courier New", Courier, monospace, "Microsoft YaHei";
      background-color: #f4d03f;
      background-image:
        radial-gradient(circle, #000 2px, transparent 2.5px);
      background-size: 30px 30px;
      color: #000;
      padding: 50px;
    }

    .container {
      min-height: 100vh;
      display: flex;
      flex-direction: column;
    }

    /* Header Section */
    .page-header {
      margin-bottom: 40px;
      display: flex;
      justify-content: space-between;
      align-items: flex-end;
      /* Align bottom */
      background: #fff;
      border: 4px solid #000;
      box-shadow: 10px 10px 0px #000;
      padding: 30px;
      transform: rotate(-1deg);
    }

    .page-title {
      font-size: 48px;
      font-weight: 900;
      text-transform: uppercase;
      line-height: 1;
      font-family: "Impact", "SimHei", sans-serif;
      text-shadow: 3px 3px 0 #d35400;
      margin-bottom: 10px;
    }

    .subtitle {
      font-size: 24px;
      background: #000;
      color: #fff;
      padding: 5px 15px;
      display: inline-block;
      font-family: monospace;
      transform: skew(-10deg);
    }

    .header-meta {
      font-family: "Consolas", monospace;
      font-weight: bold;
      font-size: 18px;
      background: #000;
      color: #fff;
      padding: 8px 16px;
      transform: rotate(2deg);
    }

    /* Content Wrapper */
    .content-wrapper {
      background: #fff;
      border: 4px solid #000;
      box-shadow: 15px 15px 0px #000;
      padding: 0;
      border-radius: 0;
    }

    table {
      width: 100%;
      border-collapse: collapse;
      font-size: 18px;
    }

    thead {
      background-color: #000;
      color: #fff;
      font-family: "SimHei", sans-serif;
    }

    th {
      padding: 20px 10px;
      text-align: center;
      font-weight: 900;
      font-size: 20px;
      border: 3px solid #000;
      text-transform: uppercase;
      letter-spacing: 1px;
    }

    td {
      padding: 15px 10px;
      text-align: center;
      vertical-align: middle;
      border: 3px solid #000;
      font-weight: bold;
    }

    /* Zebra striping */
    tbody tr:nth-child(even) {
      background-color: #ebf5fb;
    }

    tbody tr:hover {
      background-color: #d1f2eb;
    }

    /* Column Specific Styles */
    .col-index {
      font-family: "Impact", sans-serif;
      font-size: 24px;
      color: #b2bec3;
      /* Muted grey for index, or keep it bold? Let's make it bold grey */
      color: #636e72;
    }

    /* Activity Badge */
    .badge-activity {
      display: inline-block;
      padding: 6px 12px;
      background-color: #a29bfe;
      /* Purple */
      color: #fff;
      border: 2px solid #000;
      box-shadow: 3px 3px 0 #000;
      font-weight: bold;
      font-size: 16px;
    }

    /* Leader Name */
    .text-leader {
      color: #000;
      text-decoration: underline;
      text-decoration-thickness: 3px;
      text-decoration-color: #fdcb6e;
    }

    /* Content */
    .text-content {
      text-align: left;
      max-width: 400px;
      overflow: hidden;
      text-overflow: ellipsis;
      white-space: nowrap;
      font-family: "Microsoft YaHei", sans-serif;
      /* Readable for longer text */
    }

    /* Time */
    .text-time {
      font-family: "Consolas", "Monaco", monospace;
      background: #ffeaa7;
      padding: 4px 8px;
      border: 2px solid #000;
    }

    /* Cross Server Badge */
    .badge-cross {
      display: inline-block;
      padding: 4px 8px;
      background-color: #ff7675;
      /* Salmon */
      color: #000;
      border: 2px solid #000;
      font-size: 14px;
      font-weight: 900;
      transform: rotate(-10deg);
    }

    /* Footer */
    .footer-bar {
      height: 30px;
      background: repeating-linear-gradient(45deg,
          #000,
          #000 10px,
          #fff 10px,
          #fff 20px);
      border-top: 4px solid #000;
    }
  </style>
</head>

<body>
  <div class="container">

    <div class="page-header">
      <div>
        <div class="page-title">{{server}} 团队招募</div>
        <div class="subtitle">TEAM RECRUITMENT</div>
      </div>
      <div class="header-meta">
        UPDATE: {{formatTime time "YYYY-MM-DD"}}
      </div>
    </div>

    <div class="content-wrapper">
      <table>
        <thead>
          <tr>
            <th style="width: 5%">#</th>
            <th style="width: 18%">ACTIVITY</th>
            <th style="width: 7%">LV</th>
            <th style="width: 12%">LEADER</th>
            <th style="width: 8%">NUM</th>
            <th style="width: 28%">CONTENT</th>
            <th style="width: 15%">TIME</th>
            <th style="width: 7%">TAG</th>
          </tr>
        </thead>
        <tbody>
          {{#each data}}
          <tr>
            <td class="col-index">{{inc @index}}</td>
            <td>
              <span class="badge-activity">{{activity}}</span>
            </td>
            <td>{{level}}</td>
            <td class="text-leader">{{leader}}</td>
            <td>{{number}}/{{maxNumber}}</td>
            <td class="text-content" title="{{content}}">{{content}}</td>
            <td>
              <span class="text-time">{{formatTime createTime "YYYY-MM-DD"}}</span>
            </td>
            <td>
              {{#if crossServer}}
              <span class="badge-cross">跨服</span>
              {{/if}}
            </td>
          </tr>
          {{/each}}
        </tbody>
      </table>
      <div class="footer-bar"></div>
    </div>
  </div>
</body>

</html>