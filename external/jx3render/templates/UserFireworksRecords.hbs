<!DOCTYPE html>
<html lang="zh-CN">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>角色烟花记录</title>
  <style>
    @font-face {
      font-family: "DengXian";
      src: local("DengXian"), local("SimHei");
    }

    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
    }

    body {
      width: 1600px;
      margin: 0 auto;
      font-family: "Microsoft YaHei", "Heiti SC", sans-serif;
      background-color: #fceef5;
      /* Base Pink */
      background-image: radial-gradient(#ffc0cb 15%, transparent 16%), radial-gradient(#ffc0cb 15%, transparent 16%);
      background-size: 20px 20px;
      background-position: 0 0, 10px 10px;
      color: #2d3436;
      padding-bottom: 40px;
    }

    .container {
      padding: 40px;
      min-height: 100vh;
    }

    /* Header Section */
    .page-header {
      margin-bottom: 40px;
      text-align: center;
      position: relative;
    }

    .page-title {
      font-size: 42px;
      font-weight: 900;
      color: #00cec9;
      /* Teal/Blue for variation, or match others */
      text-shadow: 2px 2px 0px #fff;
      display: inline-block;
      background: #fff;
      padding: 12px 50px;
      border-radius: 60px;
      box-shadow: 0 6px 20px rgba(0, 206, 201, 0.2);
      transform: skew(-10deg);
    }

    .page-title.pink-theme {
      color: #e056fd;
      box-shadow: 0 6px 20px rgba(224, 86, 253, 0.2);
    }

    .page-title span {
      display: block;
      transform: skew(10deg);
    }

    .page-subtitle {
      margin-top: 15px;
      display: inline-block;
      background: rgba(255, 255, 255, 0.9);
      color: #636e72;
      padding: 6px 20px;
      border-radius: 20px;
      font-weight: bold;
      box-shadow: 0 2px 5px rgba(0, 0, 0, 0.05);
    }

    /* Card/Table Container */
    .card {
      background: #ffffff;
      border-radius: 24px;
      box-shadow: 0 8px 30px rgba(255, 105, 180, 0.15);
      overflow: hidden;
      padding: 10px;
    }

    .table-container {
      width: 100%;
      overflow-x: auto;
    }

    table {
      width: 100%;
      border-collapse: separate;
      border-spacing: 0;
    }

    thead {
      background-color: rgba(253, 121, 168, 0.1);
    }

    th {
      padding: 24px 16px;
      text-align: center;
      font-weight: 900;
      font-size: 20px;
      color: #e056fd;
      border-bottom: 2px solid #fd79a8;
      white-space: nowrap;
    }

    th:first-child {
      border-top-left-radius: 16px;
    }

    th:last-child {
      border-top-right-radius: 16px;
    }

    td {
      padding: 20px 16px;
      text-align: center;
      vertical-align: middle;
      font-size: 20px;
      border-bottom: 1px dashed #fab1a0;
      color: #2d3436;
      font-weight: bold;
    }

    tbody tr:last-child td {
      border-bottom: none;
    }

    tbody tr:hover {
      background-color: #fff0f6;
    }

    /* Column Styles */
    .time-badge {
      font-family: "DengXian", "Microsoft YaHei";
      color: #b2bec3;
      font-size: 18px;
    }

    .map-badge {
      background: #dfe6e9;
      color: #636e72;
      padding: 4px 12px;
      border-radius: 8px;
      font-size: 18px;
    }

    .firework-name {
      color: #d63031;
      font-weight: 900;
      position: relative;
      display: inline-block;
      padding: 5px 15px;
      background: #ffeaa7;
      border-radius: 20px;
      color: #e17055;
    }

    .sender-tag,
    .receiver-tag {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 8px;
    }

    .role-icon {
      width: 24px;
      height: 24px;
      border-radius: 50%;
      color: #fff;
      font-size: 14px;
      display: flex;
      align-items: center;
      justify-content: center;
      flex-shrink: 0;
    }

    .role-icon.sender {
      background: #fd79a8;
    }

    .role-icon.receiver {
      background: #74b9ff;
    }

    .name-text {
      font-size: 20px;
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="page-header">
      <div class="page-title pink-theme">
        <span>FIREWORKS RECORDS</span>
      </div>
      <br>
      <div class="page-subtitle">角色烟花记录</div>
    </div>

    <div class="card">
      <div class="table-container">
        <table>
          <thead>
            <tr>
              <th style="width: 15%">TIME</th>
              <th style="width: 15%">MAP</th>
              <th style="width: 30%">FIREWORK</th>
              <th style="width: 20%">SENDER</th>
              <th style="width: 20%">RECEIVER</th>
            </tr>
          </thead>
          <tbody>
            {{#each this}}
            <tr>
              <td>
                <span class="time-badge">{{formatTime time "MM-DD HH:mm"}}</span>
              </td>
              <td>
                <span class="map-badge">{{map_name}}</span>
              </td>
              <td>
                <span class="firework-name">
                  {{name}}
                </span>
              </td>
              <td>
                <div class="sender-tag">
                  <span class="role-icon sender">赠</span>
                  <span class="name-text" style="color: #fd79a8;">{{sender}}</span>
                </div>
              </td>
              <td>
                <div class="receiver-tag">
                  <span class="role-icon receiver">予</span>
                  <span class="name-text" style="color: #0984e3;">{{receive}}</span>
                </div>
              </td>
            </tr>
            {{/each}}
          </tbody>
        </table>
      </div>
    </div>
  </div>
</body>

</html>